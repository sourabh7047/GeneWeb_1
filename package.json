{
  "name": "genomeweb-backend",
  "version": "5.9.1",
  "description": "\"backend of genome project\"",
  "engines": {
    "node": "22.16.x"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "start": "node server.js",
    "client": "cd client && npm i --legacy-peer-deps && npm run dev",
    "dev": "concurrently -n 'server,client' -c 'red,green'  \"npm run start\" \"npm run client\""
  },
  "author": "\"sourabh asharma\"",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "body-parser": "^1.20.0",
    "colors": "^1.4.0",
    "concurrently": "^7.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.1",
    "firebase": "^11.9.1",
    "node": "v22.16.0",
    "node-fetch": "^2.6.6",
    "xhr2": "^0.2.1",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
